# 自动化入口剧本
1. new_page/select_page
2. analyze_target
3. search_in_scripts
4. create_hook + inject_hook
5. 触发动作
6. get_hook_data(summary)
7. 命中后 get_hook_data(raw) + get_request_initiator
8. export_session_report

重试上限：2 次。
